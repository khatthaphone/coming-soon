apiVersion: v1
kind: Service
metadata:
  name: qr-coming-soon
  namespace: saas-prod
spec:
  selector:
    app: qr-coming-soon
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
