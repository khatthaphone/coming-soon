apiVersion: apps/v1
kind: Deployment
metadata:
  name: qr-coming-soon
  namespace: saas-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: qr-coming-soon
  template:
    metadata:
      labels:
        app: qr-coming-soon
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: nginx-cache
        emptyDir: {}
      - name: nginx-run
        emptyDir: {}
      containers:
      - name: qr-coming-soon
        image: ghcr.io/khatthaphone/qr-coming-soon:cfef17313a314543f1ff39249f09c5e12f863891
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: qr-coming-soon-config
        volumeMounts:
        - name: tmp-volume
          mountPath: /tmp
        - name: nginx-cache
          mountPath: /var/cache/nginx
        - name: nginx-run
          mountPath: /var/run
        securityContext:
          runAsUser: 101
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        resources:
          requests:
            cpu: "50m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
